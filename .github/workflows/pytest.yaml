name: Pytest in uv venv
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  uv-example:
    name: python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install the project
        run: uv sync --all-extras --dev
        working-directory: .

      - name: Run tests
        run: uv run pytest tests
        working-directory: .
