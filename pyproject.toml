[project]
name = "pit"
version = "0.1.0"
description = "Minimal git functions implemented in Python"
readme = "README.md"
requires-python = ">=3.10,<=3.13"
dependencies = [
    "msgpack>=1.1.0",
]

[project.scripts]
pit = "pit.cli.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
    "pytest-sugar>=1.0.0",
    "ipykernel>=6.29.5",
    "pre-commit>=4.0.1",
    "ruff>=0.7.4",
    "pytest-cov>=6.0.0",
    "gitpython>=3.1.44",
    "pytest-freezer>=0.4.9",
]

[tool.ruff]
target-version = "py310"
lint.extend-select =["UP", "I"]

[tool.ruff.lint.pyupgrade]
# Preserve types, even if a file imports `from __future__ import annotations`.
keep-runtime-typing = true

[dependency-groups]
rich = [
    "rich>=14.0.0",
]
